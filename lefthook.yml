# Format staged files
pre-commit:
  jobs:
    - name: format
      run: bun run format {staged_files} --files-ignore-unknown true --no-errors-on-unmatched
      stage_fixed: true

# Validate commit messages
commit-msg:
  commands:
    'lint commit message':
      run: bun run commitlint --edit {1}
